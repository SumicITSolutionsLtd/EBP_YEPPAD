# Service Configuration
spring.application.name=opportunity-service
server.port=8082
  
  # Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3307/youth_connect_db
spring.datasource.username=root
spring.datasource.password=Douglas20!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
  
  # JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
  
  # Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
  
  # Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true
eureka.instance.lease-renewal-interval-in-seconds=30
  
  # Feign Client Configuration
spring.cloud.openfeign.client.config.default.connect-timeout=5000
spring.cloud.openfeign.client.config.default.read-timeout=5000
spring.cloud.openfeign.client.config.default.logger-level=full
  
  # Actuator Configuration for Monitoring
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.metrics.export.prometheus.enabled=true
  
  # Custom Application Properties
youthconnect.opportunity.serviceName=opportunity-service
youthconnect.opportunity.version=1.0.0
  
  # Opportunity Business Rules
youthconnect.opportunity.opportunity.reminderDaysBeforeDeadline=7
youthconnect.opportunity.opportunity.maxOpportunitiesPerMonth=50
youthconnect.opportunity.opportunity.autoCloseDaysAfterDeadline=3
youthconnect.opportunity.opportunity.enableAiMatching=true
youthconnect.opportunity.opportunity.minMatchingScore=0.6
  
  # Application Processing Rules
youthconnect.opportunity.application.maxApplicationsPerDay=20
youthconnect.opportunity.application.autoReviewAfterDays=14
youthconnect.opportunity.application.enableDuplicateDetection=true
youthconnect.opportunity.application.maxAttachmentsPerApplication=5
  
  # Integration Configuration
youthconnect.opportunity.integration.userServiceUrl=http://user-service
youthconnect.opportunity.integration.notificationServiceUrl=http://notification-service
youthconnect.opportunity.integration.analyticsServiceUrl=http://analytics-service
youthconnect.opportunity.integration.serviceCallTimeout=5000
youthconnect.opportunity.integration.retryAttempts=3
  
  # File Upload Configuration
youthconnect.opportunity.fileUpload.maxFileSize=10485760
youthconnect.opportunity.fileUpload.allowedExtensions=pdf,doc,docx,jpg,jpeg,png
youthconnect.opportunity.fileUpload.uploadPath=./uploads/applications
  
  # Cache Configuration
youthconnect.opportunity.cache.opportunityListTtl=300
youthconnect.opportunity.cache.opportunityDetailTtl=600
youthconnect.opportunity.cache.maxCacheSize=1000
  
  # Logging Configuration
logging.level.root=INFO
logging.level.com.youthconnect.opportunity_service=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n
  
  # API Documentation
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true